# Required Configuration
GITHUB_TOKEN=your_github_token_here  # Required: GitHub Personal Access Token
GPG_KEY_ID=your_gpg_key_id  # Required: Your GPG key ID in long format (e.g., 884E9E08D0C7DF6C)
GPG_KEY=your_gpg_private_key  # Required: Your GPG private key for signing

# Project Configuration (at least one required)
PROJECT_PATH=/path/to/your/project  # Local project path
GITHUB_REPO=username/repository  # GitHub repository to clone

# Optional Configuration
AUTH_METHOD=keyring  # Authentication method (default: keyring)

# Domain Configuration
DOMAIN=vector-weight.click  # Your domain for the terminal service
ADMIN_EMAIL=admin@vector-weight.click  # Admin email for Let's Encrypt

# Resource Limits
CPU_LIMIT=0.4  # CPU limit for warp-terminal service
MEMORY_LIMIT=512M  # Memory limit for warp-terminal service
